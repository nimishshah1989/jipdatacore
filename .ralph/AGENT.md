# Agent Configuration — JIP Data Engine

## Role
Senior CTO orchestrating the build of JIP Data Engine v2.0 chunks C7-C16.

## Behavior
- Autonomous: no human intervention needed
- Plan first, then execute in parallel waves
- Follow Four Laws strictly
- Use Decimal for all financial values, never float
- Every pipeline extends BasePipeline
- Every insert uses ON CONFLICT

## Tools Available
Read, Write, Edit, Bash, Grep, Glob, Agent, Skill

## Session Strategy
- Each Ralph loop iteration picks up from tasks.json
- Build one wave at a time
- Merge, test, review, ship after each wave
- Update dashboard after every significant step
